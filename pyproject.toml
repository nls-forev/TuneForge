[project]
name = "tuneforge"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"

dependencies = [
    "datasets>=5.0.0",
    "fastapi>=0.139.0",
    "from-root>=1.3.0",
    "pandera>=0.32.1",
]

[dependency-groups]
dev = [
    "detect-secrets>=1.5.0",
    "dvc>=3.67.1",
    "pre-commit>=4.6.0",
]
