seed: 42

dataset:
  id: lavita/AlpaCare-MedInstruct-52k
  revision: fad8593f748de481046e54f39400d5282f695280
  split: train
  testval_split_ratio: 0.2

model:
  id: meta-llama/Llama-3.1-8B-Instruct
  revision: main
  max_seq_length: 2048

generation:
  max_new_tokens: 512
  do_sample: true
  temperature: 0.7
  top_p: 0.9
  top_k: 50
  repetition_penalty: 1.0
  seeds: [42, 43, 44]

evaluation:
  n_samples: 200
  # Set this to a stable list of source row IDs to compare exactly the same
  # examples across experiments. An empty list deterministically samples IDs.
  sample_ids: []
